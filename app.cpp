#include<iostream>
#include<fstream>
#include<iomanip>
using namespace std;

int main()
{
    string currentUser = "";

    // after authentication logic => user = "anav"

    // show CRUD menu

    // perform CRUD operations for notes with userId = 1 (anavs id is 1)
    
    return 0;
}